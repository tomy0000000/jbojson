repos:
  - repo: local
    hooks:
      - id: eslint-json
        name: JSON should be formatted and sorted
        entry: eslint
        language: node
        additional_dependencies:
          - eslint@^10.3.0
          - eslint-plugin-jsonc@^3.1.2
        files: \.json$
        types: [file]
