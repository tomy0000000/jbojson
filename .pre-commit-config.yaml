repos:
  - repo: local
    hooks:
      - id: eslint-json
        name: JSON should be formatted and sorted
        entry: eslint
        language: node
        additional_dependencies:
          - eslint
          - "@eslint/json"
        files: \.json$
        types: [file]
